import pygame

# Initialize the game engine. full screen. have a circle in the middle (table)
# make it a class, that can be constructed with a list of bots
# each bot has a name. every bot is placed around the table. first bot at the top
# the bots name is displayed there
